void setup() {
  TSC_Initg; // CAPTEUR COULEUR GAUCHE
  TSC_Initd; // CAPTEUR COULEUR DROIT
  //move(1, 50 / 100.0 * 255); Le robot AVANCE à vitesse normale ici (50% de vitesse)
  }
  if(detectedColor((Vg > Rg && Vg > Bg){
    Serial.println("Vert"))
      //motor_9.run(0); STOPPER MOUVEMENT
      //motor_10.run(0); STOPPER MOUVEMENT
      //TOURNER A GAUCHE
      //_delay(1); pdt 1 sec TOURNER A GAUCHE
      //move(3, 0); TOURNER A GAUCHE
      // CONTINUER D'AVANCER à vitesse normale ic

  }else{
      //move(1, 25 / 100.0 * 255); CONTINUER D'AVANCER à vitesse normale ici (25% de vitesse)

  }
if(detectedColor((Vd > Rd && Vd > Bd){
    Serial.println("Vert"))
      //motor_9.run(0); STOPPER MOUVEMENT
      //motor_10.run(0); STOPPER MOUVEMENT
      //TOURNER A DROITE
      //_delay(1);pdt 1 sec TOURNER A DROITE
      //move(3, 0); TOURNER A DROITE
      // CONTINUER D'AVANCER à vitesse normale ic

  }else{
      //move(1, 25 / 100.0 * 255); CONTINUER D'AVANCER à vitesse normale ici (25% de vitesse)

  }
if(detectedColor((Vd > Rd && Vd > Bd)&& if(detectedColor((Vg > Rg && Vg > Bg{
    Serial.println("Vert"))
      //motor_9.run(0); STOPPER MOUVEMENT
      //motor_10.run(0); STOPPER MOUVEMENT
      //AVANCER PDT 1SEC
      //_delay(1);pdt 1 sec TOURNER A GAUCHE
      //move(3, 0); TOURNER A GAUCHE
      //AVANCER PDT 1SEC
      //_delay(1);pdt 1 sec TOURNER A GAUCHE
      //move(3, 0); TOURNER A GAUCHE
      // CONTINUER D'AVANCER à vitesse normale ic

  }else{
      //move(1, 25 / 100.0 * 255); CONTINUER D'AVANCER à vitesse normale ici (25% de vitesse)

  }
}

void _loop() {
}

void loop() {
  _loop();
}
